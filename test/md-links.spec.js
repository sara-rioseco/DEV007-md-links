import { mdLinks } from "md-links.js";


describe('mdLinks', () => {
  it('should return a promise', () => {
    expect(mdLinks()).toBe(Promise);
  });
  it('should ', () => {
    
  });
  it('should ', () => {
    
  });
  it('should ', () => {
    
  });
  it('should ', () => {
    
  });

});
